{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/dsj1984/mandrel/blob/main/.agents/schemas/lifecycle/epic.automerge.end.schema.json",
  "title": "epic.automerge.end",
  "description": "Emitted at the end of the automerge phase wrapper. merged=true means GitHub completed the squash; merged=false with a reason captures predicate-blocked or armed-but-pending outcomes.",
  "type": "object",
  "required": ["prUrl", "merged"],
  "properties": {
    "prUrl": { "type": "string", "format": "uri", "minLength": 1 },
    "merged": { "type": "boolean" },
    "reason": { "type": "string" },
    "epicId": { "type": "integer", "minimum": 1 }
  },
  "additionalProperties": false
}
