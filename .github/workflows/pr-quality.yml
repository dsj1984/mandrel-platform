# Shared PR-quality reusable workflow (Story #4).
#
# Consumable as a `workflow_call` target by any consumer repo. Tiers:
#   lint + format-check → typecheck → unit → contract → e2e/smoke →
#   migration-guard → security → osv-scan
#
# Each tier is independently enable-toggled via inputs so repos can opt out of
# tiers they haven't built yet. The aggregator job (`ci-required`) is the only
# required branch-protection context consumers need to register.
#
# Sharding: jobs that support parallelism accept a `shards` input. When
# shards > 1, configure the matrix at the call site by passing a JSON-encoded
# shard index array via the `shard-matrix` input (e.g. "[1,2,3]" for 3 shards).
# When shards == 1 (default), `shard-matrix` defaults to "[1]".
#
# SHA-pinned third-party actions.
# setup-toolchain MUST use an absolute cross-repo reference — GitHub prohibits
# relative `uses:` paths in reusable workflows called from another repository.
# Pinned to mandrel-platform-v0.3.0 (869bbbf), whose action.yml manifest is
# expression-clean. This pin is guarded by scripts/check-workflow-portability.mjs
# (Rule 3): if it ever lags onto a commit whose manifest carries a `${{ }}`
# footgun, CI fails before release (the #35 failure mode).
name: PR Quality

on:
  workflow_call:
    inputs:
      # Runner label — e.g. 'ubuntu-latest', or '["self-hosted","domio-runner"]'
      runner:
        description: >
          Runs-on label for all jobs. Single string (e.g. 'ubuntu-latest') or a
          JSON-encoded label array string (e.g. '["self-hosted","domio-runner"]').
          Defaults to ubuntu-latest.
        required: false
        type: string
        default: 'ubuntu-latest'

      # Shard count for jobs that support parallelism (unit, contract, e2e/smoke).
      shards:
        description: >
          Number of parallel shards to split the test suite across. Defaults to 1
          (no sharding). When > 1, also set shard-matrix to the matching index
          array (e.g. "[1,2,3]" for shards: 3).
        required: false
        type: number
        default: 1

      # JSON-encoded shard index array, e.g. "[1]" or "[1,2,3]".
      # Must match `shards`. Defaults to "[1]" (single shard, no parallelism).
      shard-matrix:
        description: >
          JSON-encoded array of shard indices that drives the matrix strategy for
          test jobs. Must agree with `shards` (e.g. shards:3 → shard-matrix:"[1,2,3]").
          Defaults to "[1]".
        required: false
        type: string
        default: '[1]'

      # Per-tier enable toggles.
      enable-lint:
        description: 'Enable the lint + format-check tier.'
        required: false
        type: boolean
        default: true

      enable-typecheck:
        description: 'Enable the typecheck tier.'
        required: false
        type: boolean
        default: true

      enable-unit:
        description: 'Enable the unit-test tier.'
        required: false
        type: boolean
        default: true

      enable-contract:
        description: 'Enable the contract-test tier.'
        required: false
        type: boolean
        default: true

      enable-e2e:
        description: 'Enable the e2e / smoke tier (Playwright).'
        required: false
        type: boolean
        default: true

      # Destructive-migration guard (Story #111). Platformizes the guard domio
      # and athportal each hand-rolled and swarm-os lacked. OPT-IN (default
      # false) because not every consumer ships DB migrations; repos with a
      # migration tree set this true to inherit the same PR-time invariant.
      # When enabled it becomes a `needs:` of `ci-required`, so it is
      # branch-protection-load-bearing through the single aggregator context.
      enable-migration-guard:
        description: >
          Enable the destructive-migration label guard. Scans CHANGED migration
          files for a destructive SQL/drizzle signal (DROP / ALTER … DROP /
          TRUNCATE / drizzle drop-ops) and BLOCKS the PR unless the override
          label (migration-guard-label) is applied. Opt-in: default false.
        required: false
        type: boolean
        default: false

      # The override acknowledgement label. A reviewer applies this label to a
      # PR to consciously allow a detected destructive migration. The name is
      # part of the documented contract (docs/reusable-workflows.md).
      migration-guard-label:
        description: >
          PR label that overrides a destructive-migration finding. When this
          label is on the PR, the guard reports the finding but does NOT block.
          Default 'migration:destructive-ok'.
        required: false
        type: string
        default: 'migration:destructive-ok'

      # Comma-separated migration path globs the guard scans. A changed file
      # must match one of these (or end in `.sql`) to be inspected, so an
      # unrelated source file mentioning DROP never trips the guard.
      migration-guard-globs:
        description: >
          Comma-separated migration path globs scanned by the destructive
          guard. A changed file matching one of these (or any `*.sql`) is
          inspected for a destructive signal. Default
          '**/migrations/**,**/drizzle/**'.
        required: false
        type: string
        default: '**/migrations/**,**/drizzle/**'

      # Optional coverage floor (Story #109). Off by default (0) so
      # non-adopters keep today's behaviour: pr-quality.yml already uploads
      # `**/coverage/` as an artifact, but no job asserts a floor, so a PR can
      # drop coverage while ci-required stays green. Set this to a percent and
      # the unit job fails when measured coverage falls below it — and the unit
      # job is a `needs:` of ci-required, so the floor is load-bearing. Coverage
      # is read from the EXISTING `**/coverage/coverage-summary.json` output
      # (json-summary reporter); no new tooling is required of consumers.
      coverage-threshold:
        description: >
          Minimum coverage percentage the unit job must meet. 0 (default)
          disables the gate, preserving current behaviour. When > 0, the unit
          job fails if measured coverage (read from the existing
          **/coverage/coverage-summary.json) is below this floor. The unit job
          is a needs: of ci-required, so the floor blocks the merge.
        required: false
        type: number
        default: 0

      # Which coverage metric the floor asserts when coverage-threshold > 0.
      # One of: lines | statements | functions | branches. Default 'lines'.
      coverage-metric:
        description: >
          Coverage metric the coverage-threshold floor asserts: 'lines',
          'statements', 'functions', or 'branches'. Read from total.<metric>.pct
          in coverage-summary.json. Default 'lines'. Ignored when
          coverage-threshold is 0.
        required: false
        type: string
        default: 'lines'

      # Security tier (Story #65 / MP-10). Private-repo-capable: the secret
      # scan and SAST sub-steps run a pinned binary directly and assert on its
      # exit code, so they BLOCK on a finding even on a private repo with no
      # GitHub Advanced Security (no SARIF / Code Scanning upload required).
      enable-security:
        description: >
          Enable the security tier (PR-diff secret scan + SAST). Blocking on
          findings; works on private repos without GHAS. Default true.
        required: false
        type: boolean
        default: true

      # SAST toggle nested under the security tier. Some repos run their SAST
      # via a dedicated CodeQL/GHAS path and only want the secret scan here.
      enable-sast:
        description: >
          Enable the Semgrep SAST sub-step inside the security tier. Set false
          when the repo runs SAST via a dedicated CodeQL/GHAS workflow and only
          wants the PR-diff secret scan from this tier. Default true.
        required: false
        type: boolean
        default: true

      # Semgrep ruleset config for the SAST sub-step. Defaults to 'vendored',
      # a sentinel (NOT a literal semgrep --config value) that the SAST step
      # resolves to the path of this reusable workflow's OWN checked-out copy
      # of `.semgrep/rules.json` (Story #132) — a deterministic,
      # platform-controlled snapshot of the registry ruleset 'p/default',
      # filtered to the languages this platform's reusable workflows and
      # consumer trees actually scan (js/ts/yaml/json/bash/dockerfile/
      # generic/regex). It is NOT the live registry alias: that bit us on the
      # 0.14.0 release when 'p/default' silently grew the pnpm supply-chain
      # rules + `secrets-inherit`, redding a full-tree scan with zero code
      # change on our side. See docs/reusable-workflows.md's "SAST ruleset
      # provenance" section for the bump process
      # (`scripts/update-semgrep-rules.mjs`). NOTE: 'p/ci' was rejected as an
      # alternative default — it is too narrow (0 findings on
      # eval()/command-injection fixtures). Do NOT use 'auto' here either —
      # `auto` requires metrics ON (it phones semgrep.dev to tailor the rule
      # set), incompatible with the `--metrics=off` privacy posture of the
      # SAST step. A caller MAY still override with a live registry ref (e.g.
      # 'p/security-audit') or a path of their own — that reintroduces
      # registry drift for that input, a deliberate per-caller opt-out, not
      # the platform default.
      semgrep-config:
        description: >
          Semgrep config passed to `semgrep scan --config`. Default
          'vendored' resolves to this reusable workflow's own checked-out,
          platform-controlled ruleset snapshot at `.semgrep/rules.json` (see
          docs/reusable-workflows.md) — NOT the live registry. Pass a
          registry ref (e.g. 'p/security-audit') or a path to opt back into a
          different ruleset; 'auto' is unsupported because the step runs with
          --metrics=off.
        required: false
        type: string
        default: 'vendored'

      # Additional SAST exclude globs, APPENDED to the built-in `.agents`
      # exclude (the vendored framework tree a consumer cannot edit). The
      # security tier always passes `--exclude '.agents'`; this input lets a
      # consumer also drop its own generated code / test fixtures from the SAST
      # target set without forking the workflow. Space- or comma-separated; each
      # token becomes its own `--exclude <glob>` arg. Empty (default) → only the
      # built-in `.agents` exclude applies.
      sast-exclude:
        description: >
          Extra Semgrep `--exclude` globs, appended to the built-in `.agents`
          exclude. Space- or comma-separated (e.g. 'dist coverage tests/fixtures').
          Default '' (only the vendored `.agents` framework tree is excluded).
        required: false
        type: string
        default: ''

      # Pinned gitleaks version for the secret-scan sub-step. The asset is
      # selected per-platform (darwin/linux x amd64/arm64) and verified
      # against a pinned SHA-256 before execution.
      gitleaks-version:
        description: >
          Pinned gitleaks release version (without the leading 'v') for the
          PR-diff secret scan. Default 8.30.1.
        required: false
        type: string
        default: '8.30.1'

      # Cache toggle for the setup-toolchain composite action.
      toolchain-cache:
        description: >
          Passed through to setup-toolchain's `cache` input. Set to 'false' on
          self-hosted runners that maintain their own warm pnpm store.
        required: false
        type: string
        default: 'true'

      # pnpm shim destination for self-hosted runners.
      pnpm-dest:
        description: >
          Passed through to setup-toolchain's `pnpm-dest` input. Controls where
          pnpm/action-setup installs the pnpm shim. Leave empty to use
          setup-toolchain's default (runner.temp/pnpm). Self-hosted callers
          should set this explicitly (e.g. the runner.temp/pnpm expression)
          to avoid $HOME races and git-clean eviction.
        required: false
        type: string
        default: ''

      # Egress audit (Story #112). Adds step-security/harden-runner in
      # `egress-policy: audit` mode as the FIRST step of every tier job, so each
      # consumer inherits a zero-risk network-egress baseline on every PR run.
      # Audit mode only REPORTS outbound traffic (it never blocks), so adopting
      # it cannot break a build; it can later be tightened to `block` with an
      # allowlist (see docs/reusable-workflows.md). Runner-aware: the step is
      # gated to GitHub-hosted ubuntu-latest, the only runner where the action
      # installs the monitor. On self-hosted runners (a JSON-array `runner`
      # string such as the domio/athportal labels) it is a NO-OP — harden-runner
      # deploys its agent via the runner image there, with no workflow step, so
      # running the action would be redundant at best. Default true: every
      # default ubuntu-latest consumer inherits the baseline automatically.
      enable-harden-runner:
        description: >
          Enable the step-security/harden-runner egress audit (audit mode,
          non-blocking) as the first step of each tier job. Effective only on
          the GitHub-hosted ubuntu-latest runner; a no-op on self-hosted runners
          (where harden-runner's agent ships in the runner image, not the
          workflow). Default true.
        required: false
        type: boolean
        default: true

      # OSV-scanner advisory tier (Story #114). Renovate *bumps* dependency
      # versions but never alerts on advisories for versions sitting un-bumped
      # (inside `minimumReleaseAge` or major-gated), leaving a window with no
      # live CVE signal. This tier closes it: a pinned OSV-scanner binary scans
      # the lockfile/manifest tree for known advisories. Like the secret/SAST
      # tiers it runs a PINNED, checksum-verified binary directly and asserts on
      # findings via the run log / job summary — NO SARIF / GHAS dependency — so
      # it is load-bearing on a PRIVATE repo with no GitHub Advanced Security
      # (the same constraint that made gitleaks/Semgrep the chosen gates over
      # CodeQL). Default ON: every consumer inherits the advisory signal on pin
      # bump. When enabled it is a `needs:` of `ci-required`, so the gate is
      # branch-protection-load-bearing through the single aggregator context.
      enable-osv-scan:
        description: >
          Enable the OSV-scanner advisory tier (scans the lockfile/manifest tree
          for known dependency advisories via a pinned, checksum-verified
          binary; findings surface in the run log + job summary, no SARIF/GHAS).
          Default true.
        required: false
        type: boolean
        default: true

      # Severity-to-gate threshold for the OSV-scan tier. OSV-scanner's own exit
      # code is non-zero on ANY advisory regardless of severity; this input lets
      # a consumer decide which CVSS band fails `ci-required` vs only warns. Each
      # advisory's CVSS base score (the group's `max_severity` in OSV's JSON) is
      # bucketed into critical (≥9.0) / high (≥7.0) / medium (≥4.0) / low (>0) /
      # none (unscored). A finding at or above the configured band BLOCKS the
      # job (and therefore the merge); findings below it are reported as a
      # WARNING in the job summary without failing. Set 'critical' to gate on
      # criticals only, or 'none' to fail on ANY advisory including unscored
      # ones (strictest). Default 'high'.
      osv-fail-on-severity:
        description: >
          Lowest CVSS severity band that FAILS the OSV-scan tier (and therefore
          ci-required): 'critical' (>=9.0), 'high' (>=7.0), 'medium' (>=4.0),
          'low' (>0), or 'none' (any advisory, including unscored). Advisories
          below the band are reported as warnings without blocking. Default
          'high'.
        required: false
        type: string
        default: 'high'

      # Pinned OSV-scanner version for the advisory tier. The per-platform asset
      # (darwin/linux × amd64/arm64) is verified against a pinned SHA-256 before
      # execution, mirroring the gitleaks secret-scan posture. Bump alongside
      # the checksum map in the osv-scan job when this pin moves.
      osv-scanner-version:
        description: >
          Pinned OSV-scanner release version (without the leading 'v') for the
          advisory tier. Default 2.4.0. Bump alongside the per-platform SHA-256
          map in the osv-scan job.
        required: false
        type: string
        default: '2.4.0'

    secrets:
      # Turbo remote cache credentials (athportal pattern).
      TURBO_TOKEN:
        description: 'Turbo remote cache read/write token.'
        required: false
      TURBO_TEAM:
        description: 'Turbo team slug for remote cache scoping.'
        required: false

# Permissions: callers inherit the calling workflow's token permissions.
# We declare the minimum needed here; callers may narrow further.
permissions:
  contents: read

# Cancel superseded runs on the same ref so stale PR pushes don't queue up.
concurrency:
  group: pr-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Tier 1: Lint + format-check ───────────────────────────────────────────
  lint:
    name: Lint & format
    if: ${{ inputs.enable-lint }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 15
    steps:
      # Egress audit baseline (Story #112). Audit mode reports — never blocks —
      # outbound network traffic. Gated to GitHub-hosted ubuntu-latest; a no-op
      # on self-hosted runners (harden-runner ships its agent in the runner
      # image there). MUST be the first step so it monitors all that follow.
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - name: Setup toolchain
        uses: dsj1984/mandrel-platform/.github/actions/setup-toolchain@869bbbf21faa2cdf6045d64a9c3347b928e196fe
        with:
          cache: ${{ inputs.toolchain-cache }}
          pnpm-dest: ${{ inputs.pnpm-dest }}

      - name: Lint
        run: pnpm run lint
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

      - name: Format check
        run: pnpm run format:check
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

  # ── Tier 2: Typecheck ─────────────────────────────────────────────────────
  typecheck:
    name: Typecheck
    if: ${{ inputs.enable-typecheck }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 15
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup toolchain
        uses: dsj1984/mandrel-platform/.github/actions/setup-toolchain@869bbbf21faa2cdf6045d64a9c3347b928e196fe
        with:
          cache: ${{ inputs.toolchain-cache }}
          pnpm-dest: ${{ inputs.pnpm-dest }}

      - name: Typecheck
        run: pnpm run typecheck
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

  # ── Tier 3: Unit tests ────────────────────────────────────────────────────
  unit:
    name: Unit (${{ matrix.shard }}/${{ inputs.shards }})
    if: ${{ inputs.enable-unit }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJson(inputs.shard-matrix) }}
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup toolchain
        uses: dsj1984/mandrel-platform/.github/actions/setup-toolchain@869bbbf21faa2cdf6045d64a9c3347b928e196fe
        with:
          cache: ${{ inputs.toolchain-cache }}
          pnpm-dest: ${{ inputs.pnpm-dest }}

      - name: Unit tests (shard ${{ matrix.shard }}/${{ inputs.shards }})
        run: pnpm run test:unit --shard=${{ matrix.shard }}/${{ inputs.shards }}
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

      # ── Optional coverage floor (Story #109) ──────────────────────────────
      #
      # Off by default: when `coverage-threshold` is 0 the step prints a skip
      # note and exits 0, so non-adopters keep today's behaviour. When set, it
      # reads the EXISTING `**/coverage/coverage-summary.json` (the same
      # json-summary output uploaded below) and FAILS the unit job — which is a
      # `needs:` of ci-required — when measured `total.<metric>.pct` is below the
      # floor. A set floor with NO coverage summary also fails (never a silent
      # pass on missing data). The logic is inlined as a dependency-free Node
      # script so a consumer needs NO extra file in its checkout; the platform's
      # own scripts/check-coverage-threshold.{mjs,test.mjs} are the
      # self-test of the identical algorithm.
      - name: Coverage threshold gate
        if: ${{ inputs.coverage-threshold != 0 }}
        shell: bash
        env:
          COVERAGE_THRESHOLD: ${{ inputs.coverage-threshold }}
          COVERAGE_METRIC: ${{ inputs.coverage-metric }}
        run: |
          node --input-type=module - <<'NODE'
          import { readFileSync, readdirSync, existsSync } from "node:fs";
          import { join, resolve } from "node:path";

          const threshold = Number(process.env.COVERAGE_THRESHOLD || "0");
          const metric = (process.env.COVERAGE_METRIC || "lines").trim().toLowerCase();
          const VALID = ["lines", "statements", "functions", "branches"];
          if (!Number.isFinite(threshold) || threshold < 0 || threshold > 100) {
            console.error(`[coverage-threshold] ❌ invalid threshold "${process.env.COVERAGE_THRESHOLD}"`);
            process.exit(1);
          }
          if (!VALID.includes(metric)) {
            console.error(`[coverage-threshold] ❌ invalid metric "${metric}" (expected: ${VALID.join(", ")})`);
            process.exit(1);
          }

          // Auto-scan for **/coverage/coverage-summary.json, pruning
          // node_modules + dotted dirs (.git, .agents, …).
          function find(root) {
            const out = [];
            const walk = (dir) => {
              let entries;
              try { entries = readdirSync(dir, { withFileTypes: true }); } catch { return; }
              for (const e of entries) {
                if (!e.isDirectory()) continue;
                if (e.name === "node_modules" || e.name.startsWith(".")) continue;
                const full = join(dir, e.name);
                if (e.name === "coverage") {
                  const f = join(full, "coverage-summary.json");
                  if (existsSync(f)) out.push(f);
                }
                walk(full);
              }
            };
            walk(resolve(root));
            return out;
          }

          const files = find(process.cwd());
          if (files.length === 0) {
            console.error(
              "[coverage-threshold] ❌ threshold is set but no coverage-summary.json " +
              "was found under any **/coverage/ — ensure the test step emits a " +
              "json-summary reporter (a set floor must not pass on missing data)."
            );
            process.exit(1);
          }

          let failed = false;
          for (const file of files) {
            let pct = null;
            try {
              const total = JSON.parse(readFileSync(file, "utf8"))?.total;
              const entry = total && total[metric];
              if (entry && typeof entry.pct === "number" && Number.isFinite(entry.pct)) pct = entry.pct;
            } catch { /* unreadable → treated as a failure below */ }
            if (pct === null) {
              console.error(`[coverage-threshold] ❌ ${file}: no "${metric}" total.pct in summary`);
              failed = true;
            } else if (pct >= threshold) {
              console.log(`[coverage-threshold] ✅ ${file}: ${metric} ${pct}% (floor ${threshold}%)`);
            } else {
              console.error(`[coverage-threshold] ❌ ${file}: ${metric} ${pct}% < floor ${threshold}%`);
              failed = true;
            }
          }
          if (failed) {
            console.error(`[coverage-threshold] ❌ ${metric} coverage is below the ${threshold}% floor.`);
            process.exit(1);
          }
          console.log(`[coverage-threshold] ✅ ${metric} coverage meets the ${threshold}% floor.`);
          NODE

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: unit-results-${{ matrix.shard }}
          path: |
            **/test-results/
            **/coverage/
          retention-days: 7

  # ── Tier 4: Contract tests ────────────────────────────────────────────────
  contract:
    name: Contract (${{ matrix.shard }}/${{ inputs.shards }})
    if: ${{ inputs.enable-contract }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJson(inputs.shard-matrix) }}
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup toolchain
        uses: dsj1984/mandrel-platform/.github/actions/setup-toolchain@869bbbf21faa2cdf6045d64a9c3347b928e196fe
        with:
          cache: ${{ inputs.toolchain-cache }}
          pnpm-dest: ${{ inputs.pnpm-dest }}

      - name: Contract tests (shard ${{ matrix.shard }}/${{ inputs.shards }})
        run: pnpm run test:contract --shard=${{ matrix.shard }}/${{ inputs.shards }}
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

      - name: Upload contract results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: contract-results-${{ matrix.shard }}
          path: '**/test-results/'
          retention-days: 7

  # ── Tier 5: E2E / Smoke (Playwright) ─────────────────────────────────────
  e2e:
    name: E2E / Smoke (${{ matrix.shard }}/${{ inputs.shards }})
    if: ${{ inputs.enable-e2e }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJson(inputs.shard-matrix) }}
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup toolchain
        uses: dsj1984/mandrel-platform/.github/actions/setup-toolchain@869bbbf21faa2cdf6045d64a9c3347b928e196fe
        with:
          cache: ${{ inputs.toolchain-cache }}
          pnpm-dest: ${{ inputs.pnpm-dest }}

      - name: Resolve Playwright version
        id: pw-version
        shell: bash
        run: |
          PW_VERSION=$(node -e "const p=require('./node_modules/@playwright/test/package.json');console.log(p.version)")
          echo "version=${PW_VERSION}" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw-version.outputs.version }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: pnpm exec playwright install --with-deps

      - name: Install browser OS dependencies (cache hit)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: pnpm exec playwright install-deps

      - name: E2E / Smoke (shard ${{ matrix.shard }}/${{ inputs.shards }})
        run: pnpm run test:e2e --shard=${{ matrix.shard }}/${{ inputs.shards }}
        env:
          TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
          TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: playwright-report-${{ matrix.shard }}
          path: |
            **/playwright-report/
            **/test-results/
          retention-days: 14

  # ── Destructive-migration label guard (Story #111) ───────────────────────
  #
  # Platformizes the destructive-migration guard domio and athportal each
  # hand-rolled and swarm-os was missing. OPT-IN via `enable-migration-guard`.
  # When enabled it is a `needs:` of `ci-required` (below), so it is
  # branch-protection-load-bearing through the single aggregator context.
  #
  # Contract (documented in docs/reusable-workflows.md):
  #   • Detects a destructive signal in CHANGED migration files only — DROP /
  #     ALTER … DROP / TRUNCATE / drizzle drop-ops. Comment-only mentions are
  #     stripped before matching, so a `-- DROP TABLE …` note never trips it.
  #   • A changed file is "a migration" when its path matches one of
  #     `migration-guard-globs` OR ends in `.sql`.
  #   • The override is an explicit PR label (`migration-guard-label`,
  #     default `migration:destructive-ok`). With the label present the guard
  #     reports the finding but does NOT block; absent, a finding fails the job.
  #
  # STATIC, PR-time signal only (per the Story's Out of Scope): it inspects
  # changed migration files, it does NOT introspect a live database, and it
  # does not touch the snapshot/rollback flow in deploy-cloudflare.yml.
  #
  # The detection signal set is the same union codified — and unit-tested —
  # in scripts/check-destructive-migration.mjs /
  # scripts/check-destructive-migration.test.mjs on the platform repo; this job
  # is the portable in-workflow implementation so a consumer needs no
  # consumer-side script copy to inherit the gate.
  migration-guard:
    name: Destructive-migration guard
    if: ${{ inputs.enable-migration-guard }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          # Full history so the merge-base diff resolves the PR's changed set.
          fetch-depth: 0

      # Resolve the changed migration files for this PR. On pull_request events
      # we diff merge-base..head (the commits the PR introduces); on other
      # events the guard has no PR context and is a no-op pass.
      - name: Resolve changed migration files
        id: changed
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GLOBS: ${{ inputs.migration-guard-globs }}
        run: |
          set -euo pipefail
          if [ -z "${BASE_SHA:-}" ] || [ -z "${HEAD_SHA:-}" ]; then
            echo "No pull_request context — destructive-migration guard is a no-op."
            echo "migration_files=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # `--diff-filter` keeps Added/Copied/Modified/Renamed/Deleted; a
          # DELETED migration is itself a destructive signal, so we keep D.
          # `mapfile` is a bash4+ builtin; self-hosted macOS runners ship
          # Apple's frozen bash 3.2, so build the array with a portable
          # `while read -r` loop instead (same pattern as the prior bash-3.2
          # compatibility fixes for the Semgrep SAST tier).
          changed=()
          while IFS= read -r line; do
            changed+=("$line")
          done < <(git diff --name-only --diff-filter=ACMRD "${BASE_SHA}" "${HEAD_SHA}")

          # Build a regex of `migration-guard-globs` (comma-separated), where
          # `**`→`.*` and `*`→`[^/]*`, plus an always-on `\.sql$` tail. Only
          # paths matching are inspected.
          glob_to_re() {
            local g="$1" out="" i c
            for (( i=0; i<${#g}; i++ )); do
              c="${g:$i:1}"
              if [ "$c" = "*" ]; then
                if [ "${g:$((i+1)):1}" = "*" ]; then
                  out+=".*"; i=$((i+1))
                  [ "${g:$((i+1)):1}" = "/" ] && i=$((i+1))
                else
                  out+="[^/]*"
                fi
              elif [[ "$c" =~ [.+^\$\(\)\|\[\]\\] ]]; then
                out+="\\$c"
              else
                out+="$c"
              fi
            done
            printf '^%s$' "$out"
          }

          migration_files=()
          IFS=',' read -ra glob_arr <<< "${GLOBS}"
          for f in "${changed[@]}"; do
            [ -z "$f" ] && continue
            keep=""
            if [[ "$f" =~ \.sql$ ]]; then keep="yes"; fi
            if [ -z "$keep" ]; then
              for g in "${glob_arr[@]}"; do
                g="$(echo "$g" | xargs)"   # trim
                [ -z "$g" ] && continue
                re="$(glob_to_re "$g")"
                if [[ "$f" =~ $re ]]; then keep="yes"; break; fi
              done
            fi
            [ -n "$keep" ] && migration_files+=("$f")
          done

          if [ ${#migration_files[@]} -eq 0 ]; then
            echo "No changed migration files in this PR — guard passes."
            echo "migration_files=" >> "$GITHUB_OUTPUT"
          else
            printf 'Changed migration files:\n'
            printf '  %s\n' "${migration_files[@]}"
            # Newline-join into a heredoc-delimited multiline output.
            {
              echo "migration_files<<__MIGEOF__"
              printf '%s\n' "${migration_files[@]}"
              echo "__MIGEOF__"
            } >> "$GITHUB_OUTPUT"
          fi

      # Scan the changed migration files for a destructive signal. The signal
      # set mirrors scripts/check-destructive-migration.mjs (DROP / ALTER … DROP
      # / TRUNCATE / drizzle drop-ops), with SQL/JS comments stripped first so a
      # commented-out DROP does not trip the guard. The override label, when
      # present on the PR, downgrades a finding from a block to a warning.
      - name: Scan for destructive migrations
        if: ${{ steps.changed.outputs.migration_files != '' }}
        shell: bash
        env:
          MIGRATION_FILES: ${{ steps.changed.outputs.migration_files }}
          OVERRIDE_LABEL: ${{ inputs.migration-guard-label }}
          # PR labels as a JSON array string. We parse + match exactly with jq
          # (jq ships on ubuntu-latest and the self-hosted runners), avoiding
          # both substring-collision false positives from a `contains()` match
          # and the YAML fragility of an embedded-newline join.
          PR_LABELS_JSON: ${{ toJSON(github.event.pull_request.labels.*.name) }}
        run: |
          set -euo pipefail

          # Strip `--`, `//`, and inline `/* */` comments from a line.
          strip_comments() {
            sed -E 's:/\*.*\*/: :g; s:--.*$::; s://.*$::'
          }

          findings=""
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            if [ ! -f "$f" ]; then
              # Deleted migration file — destructive by itself.
              findings+="  • ${f} → deleted migration file"$'\n'
              continue
            fi
            sigs=""
            stripped="$(strip_comments < "$f")"
            echo "$stripped" | grep -qiE '\bALTER[[:space:]]+TABLE\b.*\bDROP\b'              && sigs+="ALTER TABLE … DROP; "
            echo "$stripped" | grep -qiE '\bDROP[[:space:]]+(TABLE|COLUMN|INDEX|SCHEMA|CONSTRAINT|VIEW|DATABASE|TYPE|SEQUENCE|TRIGGER|FUNCTION)\b' && sigs+="DROP statement; "
            echo "$stripped" | grep -qiE '\bTRUNCATE\b'                                      && sigs+="TRUNCATE; "
            echo "$stripped" | grep -qE  '\.(dropTable|dropColumn|dropIndex|dropConstraint|dropForeignKey|dropPrimaryKey|dropUnique)[[:space:]]*\(' && sigs+="drizzle destructive op; "
            [ -n "$sigs" ] && findings+="  • ${f} → ${sigs%; }"$'\n'
          done <<< "${MIGRATION_FILES}"

          if [ -z "$findings" ]; then
            echo "✅ No destructive migration detected in the changed files."
            exit 0
          fi

          # A destructive signal is present. Honour the override label: exact
          # match of OVERRIDE_LABEL against the PR's label-name array.
          if printf '%s' "${PR_LABELS_JSON}" \
            | jq -e --arg l "${OVERRIDE_LABEL}" 'any(.[]?; . == $l)' >/dev/null 2>&1; then
            label_present="yes"
          else
            label_present="no"
          fi

          echo "Destructive migration signal(s):"
          printf '%s' "$findings"

          if [ "$label_present" = "yes" ]; then
            echo "⚠️ Override label '${OVERRIDE_LABEL}' is applied — allowing the destructive migration."
            {
              echo "### Destructive-migration guard — ALLOWED via override"
              echo ""
              echo "Override label \`${OVERRIDE_LABEL}\` is present. Findings:"
              echo ""
              printf '%s' "$findings"
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          {
            echo "### ❌ Destructive-migration guard — BLOCKED"
            echo ""
            echo "A destructive migration was detected and the override label"
            echo "\`${OVERRIDE_LABEL}\` is NOT applied. Findings:"
            echo ""
            printf '%s' "$findings"
            echo ""
            echo "A reviewer must apply the \`${OVERRIDE_LABEL}\` label to"
            echo "acknowledge the destructive change, then re-run this check."
          } >> "$GITHUB_STEP_SUMMARY"

          echo "::error::Destructive migration detected without the '${OVERRIDE_LABEL}' override label." >&2
          exit 1

  # ── Tier 6: Security (PR-diff secret scan + SAST) ─────────────────────────
  #
  # Private-repo-capable security tier (Story #65 / MP-10). Both sub-steps run
  # a pinned binary directly and BLOCK the job on a finding via a non-zero
  # exit code — no SARIF / Code Scanning upload, so the gate is load-bearing on
  # a PRIVATE repo with NO GitHub Advanced Security (the failure mode that left
  # athportal's `gitleaks-pr` + CodeQL green no-ops and stripped swarm-os's CI
  # secret scan). Cross-platform: the gitleaks asset selector resolves the
  # right binary for self-hosted darwin/linux runners AND ubuntu-latest, each
  # verified against a pinned SHA-256.
  #
  #   • Secret scan — pinned gitleaks binary over the PR diff (blocking).
  #   • SAST        — pinned Semgrep, `--error` fails the job on any finding
  #                   (blocking, no SARIF upload). On pull_request events it is
  #                   scoped to the PR diff via `--baseline-commit <base.sha>`
  #                   (mirroring the secret scan), so only findings INTRODUCED
  #                   by the PR block; on push / schedule it scans the full tree.
  #                   The vendored framework tree `.agents/` is ALWAYS excluded
  #                   (`--exclude '.agents'`): it is materialized from the pinned
  #                   `mandrel` package and a consumer cannot edit it, so gating
  #                   `ci-required` on the framework's own findings is a false
  #                   block (Story #93). Consumers can drop their own generated
  #                   code / fixtures too via the `sast-exclude` input, which is
  #                   APPENDED to the built-in `.agents` exclude.
  #
  # GHAS path (alternative, documented per AC): repos that DO have GitHub
  # Advanced Security can instead run CodeQL (codeql.yml) and flip its
  # `upload: never` → `always` to surface findings as a blocking Code Scanning
  # check. This tier is the no-GHAS-required default so every consumer inherits
  # an effective gate regardless of plan.
  security:
    name: Security (secret scan + SAST)
    if: ${{ inputs.enable-security }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          # Full history so the gitleaks PR-diff scan can resolve the merge
          # base; a shallow clone would miss commits introduced by the PR.
          fetch-depth: 0

      # Resolve the gitleaks PR-diff commit range. On pull_request events the
      # scan is scoped to the commits the PR introduces (merge-base..head); on
      # push / other events it falls back to a full-tree scan.
      - name: Resolve secret-scan range
        id: range
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if [ -n "${BASE_SHA:-}" ] && [ -n "${HEAD_SHA:-}" ]; then
            echo "mode=pr" >> "$GITHUB_OUTPUT"
            echo "range=${BASE_SHA}..${HEAD_SHA}" >> "$GITHUB_OUTPUT"
            echo "PR-diff secret scan: ${BASE_SHA}..${HEAD_SHA}"
          else
            echo "mode=full" >> "$GITHUB_OUTPUT"
            echo "range=" >> "$GITHUB_OUTPUT"
            echo "Full-tree secret scan (no PR context)."
          fi

      # Download + verify a PINNED gitleaks binary. Cross-platform asset
      # selector: maps runner.os/runner.arch → the release asset, then verifies
      # the archive against a pinned SHA-256 before extraction. Supports
      # self-hosted darwin/linux (arm64 + x64) and ubuntu-latest. Pinning the
      # binary (not the gitleaks-action) is what makes this gate work on a
      # private repo with no GHAS and no license: the action's org features
      # require GITLEAKS_LICENSE, the binary does not.
      - name: Secret scan (pinned gitleaks, blocking)
        shell: bash
        env:
          GITLEAKS_VERSION: ${{ inputs.gitleaks-version }}
          SCAN_MODE: ${{ steps.range.outputs.mode }}
          SCAN_RANGE: ${{ steps.range.outputs.range }}
        run: |
          set -euo pipefail

          # Pinned SHA-256 checksums for gitleaks 8.30.1 release archives.
          # Bump alongside inputs.gitleaks-version when the pin moves.
          # Uses a case statement instead of declare -A to remain compatible
          # with bash 3.2 (the system shell on macOS self-hosted runners).

          # Cross-platform asset selector: runner.os/arch → gitleaks asset slug.
          os="$(uname -s)"
          arch="$(uname -m)"
          case "$os" in
            Darwin) gl_os="darwin" ;;
            Linux)  gl_os="linux" ;;
            *) echo "::error::Unsupported OS for gitleaks asset selector: $os"; exit 1 ;;
          esac
          case "$arch" in
            arm64|aarch64) gl_arch="arm64" ;;
            x86_64|amd64)  gl_arch="x64" ;;
            *) echo "::error::Unsupported arch for gitleaks asset selector: $arch"; exit 1 ;;
          esac

          slug="${GITLEAKS_VERSION}_${gl_os}_${gl_arch}"
          case "$slug" in
            "8.30.1_darwin_arm64") expected="b40ab0ae55c505963e365f271a8d3846efbc170aa17f2607f13df610a9aeb6a5" ;;
            "8.30.1_darwin_x64")   expected="dfe101a4db2255fc85120ac7f3d25e4342c3c20cf749f2c20a18081af1952709" ;;
            "8.30.1_linux_arm64")  expected="e4a487ee7ccd7d3a7f7ec08657610aa3606637dab924210b3aee62570fb4b080" ;;
            "8.30.1_linux_x64")    expected="551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb" ;;
            *) expected="" ;;
          esac
          if [ -z "$expected" ]; then
            echo "::error::No pinned checksum for gitleaks $slug — update the SHA256 map for this version/platform."
            exit 1
          fi

          asset="gitleaks_${GITLEAKS_VERSION}_${gl_os}_${gl_arch}.tar.gz"
          url="https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/${asset}"
          tmp="$(mktemp -d)"
          echo "Downloading pinned gitleaks: ${url}"
          curl -fsSL "$url" -o "${tmp}/${asset}"

          # Verify the pinned SHA-256 before trusting the binary.
          actual="$(shasum -a 256 "${tmp}/${asset}" | awk '{print $1}')"
          if [ "$actual" != "$expected" ]; then
            echo "::error::gitleaks checksum mismatch for ${asset}: expected ${expected}, got ${actual}"
            exit 1
          fi
          echo "✅ gitleaks ${asset} checksum verified."

          tar -xzf "${tmp}/${asset}" -C "${tmp}" gitleaks
          chmod +x "${tmp}/gitleaks"
          "${tmp}/gitleaks" version

          # Blocking scan: gitleaks exits non-zero on a leak (default exit-code
          # 1), failing this job. No license, no SARIF upload — works on a
          # private repo with no GHAS.
          # --redact=100 (not bare --redact, whose optional uint would otherwise
          # swallow the trailing '.' path arg). Exit code 1 on a leak (gitleaks
          # default) fails this job.
          if [ "$SCAN_MODE" = "pr" ]; then
            echo "Scanning PR diff range: ${SCAN_RANGE}"
            "${tmp}/gitleaks" git --log-opts="${SCAN_RANGE}" --redact=100 --verbose --no-banner .
          else
            echo "Scanning full working tree."
            "${tmp}/gitleaks" dir --redact=100 --verbose --no-banner .
          fi
          echo "✅ No secrets detected."

      # Check out THIS reusable workflow's own repo (mandrel-platform) at the
      # exact SHA the caller's `uses: …/pr-quality.yml@<ref>` pin resolved to
      # (`github.job_workflow_sha` — the same resolved-ref single-source-of-
      # truth primitive `deploy-summary` echoes in deploy-cloudflare.yml,
      # Story #110), into a side directory `_mandrel-platform-sast/`. The
      # caller's own checkout (above) never contains `.semgrep/rules.json` —
      # that file lives in THIS repo, not the consumer's — so the vendored
      # ruleset has to come from a second, explicit checkout rather than the
      # primary one. Resolving via `job_workflow_sha` (not a hand-maintained
      # ref literal) means a consumer always gets the ruleset snapshot that
      # shipped with the exact pr-quality.yml version it pinned — no drift
      # between "which workflow ran" and "which ruleset it scanned with".
      - name: Checkout vendored SAST ruleset (mandrel-platform@resolved-sha)
        if: ${{ inputs.enable-sast && inputs.semgrep-config == 'vendored' }}
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: dsj1984/mandrel-platform
          # `job_workflow_sha` is the resolved 40-hex commit of THIS reusable
          # workflow (the value the caller's `@<ref>` pin resolved to). It is
          # a valid runtime github-context field, but actionlint's pinned
          # context type model does not list it, so it is read through the
          # `fromJSON(toJSON(github))` escape hatch (typed `any`) to keep the
          # workflow lint-clean — same pattern as deploy-summary in
          # deploy-cloudflare.yml (Story #110).
          ref: ${{ fromJSON(toJSON(github)).job_workflow_sha }}
          sparse-checkout: |
            .semgrep
          sparse-checkout-cone-mode: false
          path: _mandrel-platform-sast
          persist-credentials: false

      # Resolve the Semgrep SAST baseline, mirroring the gitleaks PR-diff
      # scoping above. On pull_request events the baseline is the PR's merge
      # base (`base.sha`), so `--baseline-commit` makes Semgrep report only
      # findings INTRODUCED by the PR (pre-existing debt is suppressed). On
      # push / schedule / other events the baseline is empty → full-tree scan.
      # `fetch-depth: 0` on the checkout above makes the base commit available.
      - name: Resolve SAST baseline
        id: sast-baseline
        if: ${{ inputs.enable-sast }}
        shell: bash
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          if [ -n "${BASE_SHA:-}" ]; then
            echo "baseline=${BASE_SHA}" >> "$GITHUB_OUTPUT"
            echo "PR-diff SAST baseline: ${BASE_SHA}"
          else
            echo "baseline=" >> "$GITHUB_OUTPUT"
            echo "Full-tree SAST (no PR context)."
          fi

      # SAST via Semgrep, installed from PyPI at a pinned version against the
      # runner's SYSTEM python3. We deliberately do NOT use
      # `actions/setup-python`: on a vanilla self-hosted runner with no
      # RUNNER_TOOL_CACHE configured, setup-python falls back to the
      # GitHub-hosted tool-cache path (`/Users/runner/hostedtoolcache` on
      # macOS) and dies `mkdir: /Users/runner: Permission denied` — before
      # Semgrep ever runs (Story #85: domio #1550 / athportal #2021). Both
      # ubuntu-latest and macOS/Linux self-hosted runners ship a usable
      # `python3`, so installing against it keeps the gate runner-agnostic
      # with no per-runner tool-cache config. `--error` makes findings fail the
      # job WITHOUT relying on a SARIF / Code Scanning upload, so the gate
      # blocks on a private repo with no GHAS. (The GHAS CodeQL path documented
      # on the job header is the alternative.)
      - name: SAST (Semgrep, blocking, no SARIF upload)
        if: ${{ inputs.enable-sast }}
        shell: bash
        env:
          SEMGREP_CONFIG: ${{ inputs.semgrep-config }}
          # PR merge base on pull_request events, empty otherwise. When set,
          # `--baseline-commit` scopes findings to those introduced by the PR.
          BASELINE: ${{ steps.sast-baseline.outputs.baseline }}
          # Extra exclude globs appended to the built-in `.agents` exclude.
          # Space- or comma-separated; empty by default.
          SAST_EXCLUDE: ${{ inputs.sast-exclude }}
        run: |
          set -euo pipefail

          # Resolve the 'vendored' sentinel (Story #132) to the path of the
          # side-checked-out platform ruleset from the step above; any other
          # value (a registry ref like 'p/security-audit' or a caller-owned
          # path) passes through unchanged — that is the deliberate
          # per-caller opt-out of the platform-controlled default.
          if [ "${SEMGREP_CONFIG}" = "vendored" ]; then
            SEMGREP_CONFIG="${GITHUB_WORKSPACE}/_mandrel-platform-sast/.semgrep/rules.json"
            if [ ! -f "${SEMGREP_CONFIG}" ]; then
              echo "::error::Vendored ruleset not found at ${SEMGREP_CONFIG} — the 'Checkout vendored SAST ruleset' step must run before this one when semgrep-config is 'vendored'."
              exit 1
            fi
          fi

          # Pinned Semgrep release from PyPI. `semgrep scan --error` exits
          # non-zero on any finding; `--config` selects the ruleset (default:
          # this reusable workflow's own vendored `.semgrep/rules.json`, a
          # deterministic snapshot — NOT the live registry alias
          # 'p/default'). Bump the ruleset deliberately via
          # `scripts/update-semgrep-rules.mjs` (see
          # docs/reusable-workflows.md).
          #
          # `setuptools` provides `pkg_resources`, which semgrep's transitive
          # opentelemetry dependency imports but does not declare — absent by
          # default on Python ≥3.12. We install it alongside the pinned
          # semgrep so the run works on a 3.12+ system interpreter too.
          #
          # Install strategy (system-python, no hosted tool cache):
          #   • Prefer `pipx` when present — it isolates the install in its own
          #     venv and exposes `semgrep` on PATH, without polluting the
          #     system site-packages. We `--system-site-packages` so the venv
          #     can see a system `setuptools`, then `pipx inject` a pinned one
          #     as a belt-and-suspenders guarantee for `pkg_resources`.
          #   • Otherwise fall back to an EPHEMERAL `python3 -m venv` (Story
          #     #92). We deliberately do NOT use `pip install --user`: `--user`
          #     installs into the runner's SHARED, MUTABLE user site-packages
          #     (`~/Library/Python/X.Y/lib/python/site-packages` on macOS). On
          #     a self-hosted host whose user-site already holds a compiled
          #     native extension built for a DIFFERENT arch (e.g. a prior
          #     native-arm64 Python left an arm64 `rpds.cpython-39-darwin.so`),
          #     a later x86_64/Rosetta run REUSES that stale `.so` instead of
          #     rebuilding it, and Semgrep dies at IMPORT before it scans:
          #       `mach-o file, but is an incompatible architecture
          #        (have 'arm64', need 'x86_64')`.
          #     A venv disables user-site by default (PEP 370) and rebuilds
          #     native deps for the venv interpreter's own arch, so the install
          #     is hermetic and arch-correct on a vanilla, possibly-dirty
          #     self-hosted runner — no per-runner `rpds` purge needed
          #     (athportal #2021 / PR #2023). The venv lives under a `mktemp -d`
          #     dir, so it is per-run and never reads/writes the shared user
          #     site. No `pipx` dependency.
          SEMGREP_PIN='semgrep==1.97.0'

          # Build the `--exclude` argument list. The vendored framework tree
          # `.agents/` is ALWAYS excluded: it is materialized from the pinned
          # `mandrel` package and a consumer cannot edit it, so gating on its
          # findings is a false block (Story #93). Semgrep's `--exclude` takes a
          # pattern matched against path components, so `--exclude '.agents'`
          # drops the whole `.agents/**` subtree from the target set (mirroring
          # how it ignores `node_modules` by default). `_mandrel-platform-sast`
          # is the side-checkout directory the 'vendored' ruleset resolution
          # step (above) materializes to carry `.semgrep/rules.json` — it is
          # mandrel-platform's OWN source tree, not the caller's, and must
          # never enter this caller's finding set regardless of which
          # `semgrep-config` value is in effect (the directory only exists
          # when `semgrep-config == 'vendored'`, but excluding it
          # unconditionally is harmless and keeps this list static). Any
          # extra globs from the `sast-exclude` input are APPENDED, each as
          # its own `--exclude` arg.
          EXCLUDE_ARGS=(--exclude '.agents' --exclude '_mandrel-platform-sast')
          if [ -n "${SAST_EXCLUDE:-}" ]; then
            # Split on whitespace AND commas so 'dist,coverage tests/fixtures'
            # all work. Empty tokens (e.g. from a trailing comma) are skipped.
            old_ifs="$IFS"
            IFS=', '
            for glob in ${SAST_EXCLUDE}; do
              [ -n "$glob" ] && EXCLUDE_ARGS+=(--exclude "$glob")
            done
            IFS="$old_ifs"
          fi

          run_semgrep() {
            echo "Running $(semgrep --version 2>/dev/null) (config: ${SEMGREP_CONFIG})"
            if [ -n "${BASELINE:-}" ]; then
              echo "PR-diff SAST: baselining against ${BASELINE} (only new findings block)."
            else
              echo "Full-tree SAST (no baseline)."
            fi
            echo "SAST excludes: ${EXCLUDE_ARGS[*]}"
            # `${BASELINE:+--baseline-commit "$BASELINE"}` expands to the flag
            # pair only when BASELINE is non-empty; on push/schedule it vanishes
            # and Semgrep scans the full tree. `"${EXCLUDE_ARGS[@]}"` always
            # carries at least `--exclude '.agents'` (Story #93), plus any globs
            # from the `sast-exclude` input, on BOTH the baseline and full-tree
            # paths since both flow through this one invocation.
            semgrep scan \
              --config "${SEMGREP_CONFIG}" \
              "${EXCLUDE_ARGS[@]}" \
              ${BASELINE:+--baseline-commit "$BASELINE"} \
              --error \
              --disable-version-check \
              --metrics=off \
              --quiet
          }

          if command -v pipx >/dev/null 2>&1; then
            echo "Installing ${SEMGREP_PIN} via pipx (system python3)."
            # `--force` keeps the step idempotent across reused self-hosted
            # runners (a prior `pipx install semgrep` would otherwise abort
            # with "already installed").
            pipx install --force --pip-args='--disable-pip-version-check' \
              --system-site-packages "${SEMGREP_PIN}"
            pipx inject semgrep setuptools >/dev/null 2>&1 || true
            # pipx exposes apps under ~/.local/bin, which may not be on PATH on
            # a fresh runner. `pipx ensurepath` is interactive-shell-oriented;
            # prepend the bin dir directly so `semgrep` resolves this step.
            PATH="${HOME}/.local/bin:${PATH}"
            export PATH
            run_semgrep
          else
            echo "pipx not found; installing ${SEMGREP_PIN} into an ephemeral python3 venv (hermetic, arch-correct)."
            # Ephemeral, per-run venv under a fresh mktemp dir. venvs disable
            # user-site by default (PEP 370), so a stale / foreign-arch native
            # extension in the runner's shared user site-packages (the Story #92
            # rpds arm64-vs-x86_64 failure) is NOT read — pip rebuilds native
            # deps for THIS interpreter's arch into the isolated venv. No
            # reliance on the shared `--user` site.
            venv_dir="$(mktemp -d)/semgrep-venv"
            python3 -m venv "${venv_dir}"
            # Use the venv's own python/pip directly (no `activate` needed; the
            # venv interpreter resolves its own isolated site-packages). Pin
            # `setuptools` alongside semgrep for the `pkg_resources` guarantee.
            "${venv_dir}/bin/python" -m pip install --quiet --disable-pip-version-check \
              setuptools "${SEMGREP_PIN}"
            # Put the venv's bin dir first so the `semgrep` resolved by
            # `run_semgrep` is the hermetic one, not any shared/user-site copy.
            PATH="${venv_dir}/bin:${PATH}"
            export PATH
            run_semgrep
          fi
          echo "✅ Semgrep found no blocking findings."

  # ── Tier 7: OSV-scanner advisory scan ─────────────────────────────────────
  #
  # Dependency-advisory tier (Story #114). Renovate bumps versions but never
  # alerts on advisories for versions sitting un-bumped (inside
  # `minimumReleaseAge` or major-gated), so there is a window with no live CVE
  # signal. This tier closes it: a PINNED, checksum-verified OSV-scanner binary
  # scans the lockfile/manifest tree (`scan source -r .`) against the OSV
  # database and reports known advisories. Like the secret/SAST tiers it runs
  # the binary directly and asserts on findings via the run log / job summary —
  # NO SARIF / Code Scanning upload — so the gate is load-bearing on a PRIVATE
  # repo with NO GitHub Advanced Security (the same constraint that made
  # gitleaks/Semgrep the chosen gates over CodeQL). Default ON
  # (`enable-osv-scan`), so every consumer inherits the advisory signal on pin
  # bump. When enabled it is a `needs:` of `ci-required` (below), so it is
  # branch-protection-load-bearing through the single aggregator context.
  #
  # Severity-to-gate (documented in docs/reusable-workflows.md):
  #   • OSV-scanner's own exit code is non-zero on ANY advisory regardless of
  #     severity. We do NOT rely on it for the gate decision; instead we emit
  #     JSON, bucket each advisory's CVSS base score (the group's
  #     `max_severity`) into critical (≥9.0) / high (≥7.0) / medium (≥4.0) /
  #     low (>0) / none (unscored), and FAIL the job only when a finding lands
  #     at or above `osv-fail-on-severity` (default 'high'). Findings below the
  #     band are reported as a WARNING in the job summary without blocking.
  #   • A human-readable table is always written to $GITHUB_STEP_SUMMARY and
  #     the run log, so findings surface with no GHAS dependency.
  #
  # Cross-platform: the asset selector resolves the right binary for self-hosted
  # darwin/linux runners AND ubuntu-latest, each verified against a pinned
  # SHA-256 before execution (mirroring the gitleaks secret-scan tier).
  osv-scan:
    name: OSV advisory scan
    if: ${{ inputs.enable-osv-scan }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Harden runner (egress audit)
        if: ${{ inputs.enable-harden-runner && inputs.runner == 'ubuntu-latest' }}
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # Download + verify a PINNED OSV-scanner binary. Cross-platform asset
      # selector: maps runner.os/runner.arch → the release asset, then verifies
      # it against a pinned SHA-256 before execution. Supports self-hosted
      # darwin/linux (arm64 + x64) and ubuntu-latest. Pinning the binary (not an
      # action) keeps the gate SHA-action-ratchet-exempt and GHAS-free, exactly
      # like the gitleaks secret-scan step.
      - name: OSV advisory scan (pinned binary)
        shell: bash
        env:
          OSV_VERSION: ${{ inputs.osv-scanner-version }}
          OSV_FAIL_ON: ${{ inputs.osv-fail-on-severity }}
        run: |
          set -euo pipefail

          # Cross-platform asset selector: runner.os/arch → OSV-scanner asset
          # slug. OSV-scanner ships bare per-platform executables (no archive).
          os="$(uname -s)"
          arch="$(uname -m)"
          case "$os" in
            Darwin) osv_os="darwin" ;;
            Linux)  osv_os="linux" ;;
            *) echo "::error::Unsupported OS for OSV-scanner asset selector: $os"; exit 1 ;;
          esac
          case "$arch" in
            arm64|aarch64) osv_arch="arm64" ;;
            x86_64|amd64)  osv_arch="amd64" ;;
            *) echo "::error::Unsupported arch for OSV-scanner asset selector: $arch"; exit 1 ;;
          esac

          # Pinned SHA-256 checksums for OSV-scanner 2.4.0 release binaries.
          # Bump alongside inputs.osv-scanner-version when the pin moves. Uses a
          # case statement (not declare -A) to remain compatible with bash 3.2
          # (the system shell on macOS self-hosted runners).
          slug="${OSV_VERSION}_${osv_os}_${osv_arch}"
          case "$slug" in
            "2.4.0_darwin_amd64") expected="088119325156321c34c456ac3703d6013538fd71cbac82b891ab34db491e4d66" ;;
            "2.4.0_darwin_arm64") expected="9ca3185ad63e9ab54f7cb90f46a7362be02d80e37f0123d095a54355ea202f5d" ;;
            "2.4.0_linux_amd64")  expected="15314940c10d26af9c6649f150b8a47c1262e8fc7e17b1d1029b0e479e8ed8a0" ;;
            "2.4.0_linux_arm64")  expected="44e580752910f0ff36ec99aff59af20f65df1e859aa31e5605a8f0d055b496e9" ;;
            *) expected="" ;;
          esac
          if [ -z "$expected" ]; then
            echo "::error::No pinned checksum for OSV-scanner $slug — update the SHA256 map for this version/platform."
            exit 1
          fi

          asset="osv-scanner_${osv_os}_${osv_arch}"
          url="https://github.com/google/osv-scanner/releases/download/v${OSV_VERSION}/${asset}"
          tmp="$(mktemp -d)"
          echo "Downloading pinned OSV-scanner: ${url}"
          curl -fsSL "$url" -o "${tmp}/osv-scanner"

          # Verify the pinned SHA-256 before trusting the binary.
          actual="$(shasum -a 256 "${tmp}/osv-scanner" | awk '{print $1}')"
          if [ "$actual" != "$expected" ]; then
            echo "::error::OSV-scanner checksum mismatch for ${asset}: expected ${expected}, got ${actual}"
            exit 1
          fi
          echo "✅ OSV-scanner ${asset} checksum verified."
          chmod +x "${tmp}/osv-scanner"
          "${tmp}/osv-scanner" --version

          # Scan the lockfile/manifest tree recursively. OSV-scanner exits
          # non-zero on ANY finding (and exit 128 on no scannable target); we
          # capture exit code + JSON and decide the gate ourselves from the
          # severity band, so `|| true` here is intentional — the JSON gate
          # below owns pass/fail, not OSV-scanner's coarse exit code.
          report="${tmp}/osv-report.json"
          echo "Running: osv-scanner scan source -r . --format json"
          "${tmp}/osv-scanner" scan source -r . --format json --output-file "${report}" || scan_rc=$?
          scan_rc="${scan_rc:-0}"
          echo "OSV-scanner raw exit code: ${scan_rc}"

          # Exit 127/128 (no scannable target) is treated as a clean pass: a
          # repo with no recognized lockfile/manifest has nothing to advise on.
          if [ ! -s "${report}" ]; then
            echo "No OSV JSON report produced (no scannable lockfile/manifest) — advisory scan is a no-op pass."
            {
              echo "### OSV advisory scan — no scannable target"
              echo ""
              echo "OSV-scanner found no recognized lockfile/manifest to scan; nothing to advise on."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          # Severity-band gate. Parse the JSON, bucket each advisory by its
          # group `max_severity` (CVSS base score string), and fail only when a
          # finding lands at or above OSV_FAIL_ON. Inlined as a dependency-free
          # Node script so a consumer needs NO extra file in its checkout.
          OSV_REPORT="${report}" node --input-type=module - <<'NODE'
          import { readFileSync } from "node:fs";

          const failOn = (process.env.OSV_FAIL_ON || "high").trim().toLowerCase();
          const BANDS = ["none", "low", "medium", "high", "critical"];
          if (!BANDS.includes(failOn)) {
            console.error(`::error::Invalid osv-fail-on-severity "${failOn}" (expected: ${BANDS.join(", ")}).`);
            process.exit(1);
          }
          // Rank: a finding blocks when its band rank >= the threshold rank.
          const rank = (b) => BANDS.indexOf(b);

          // CVSS base score → severity band.
          const bandOf = (score) => {
            if (!Number.isFinite(score) || score <= 0) return "none";
            if (score >= 9.0) return "critical";
            if (score >= 7.0) return "high";
            if (score >= 4.0) return "medium";
            return "low";
          };

          let report;
          try {
            report = JSON.parse(readFileSync(process.env.OSV_REPORT, "utf8"));
          } catch (e) {
            console.error(`::error::Could not parse OSV JSON report: ${e.message}`);
            process.exit(1);
          }

          const rows = [];
          for (const res of report.results || []) {
            const source = res.source?.path || "(unknown source)";
            for (const pkg of res.packages || []) {
              const name = pkg.package?.name || "(unknown)";
              const version = pkg.package?.version || "?";
              const ecosystem = pkg.package?.ecosystem || "";
              // Each group bundles aliased advisories with a single
              // `max_severity` CVSS base score string; ids are the OSV ids.
              for (const group of pkg.groups || []) {
                const score = Number.parseFloat(group.max_severity ?? "");
                const band = bandOf(score);
                const ids = (group.ids || []).join(", ");
                rows.push({ source, name, version, ecosystem, band, score, ids });
              }
              // Defensive: an advisory not surfaced via a group still counts as
              // an unscored ("none") finding so it cannot silently vanish.
              if ((pkg.groups || []).length === 0) {
                for (const v of pkg.vulnerabilities || []) {
                  rows.push({ source, name, version, ecosystem, band: "none", score: NaN, ids: v.id || "(unknown)" });
                }
              }
            }
          }

          const summaryPath = process.env.GITHUB_STEP_SUMMARY;
          const fs = await import("node:fs");
          const emit = (line) => { if (summaryPath) fs.appendFileSync(summaryPath, line + "\n"); console.log(line); };

          if (rows.length === 0) {
            emit("### ✅ OSV advisory scan — no known advisories");
            emit("");
            emit("OSV-scanner found no known advisories in the lockfile/manifest tree.");
            process.exit(0);
          }

          // Sort most-severe first for the table.
          rows.sort((a, b) => rank(b.band) - rank(a.band) || (b.score || 0) - (a.score || 0));

          const blocking = rows.filter((r) => rank(r.band) >= rank(failOn));
          const warning = rows.filter((r) => rank(r.band) < rank(failOn));

          const verdict = blocking.length > 0 ? "❌ BLOCKED" : "⚠️ advisories found (below gate)";
          emit(`### OSV advisory scan — ${verdict}`);
          emit("");
          emit(`Gate: fail on **${failOn}** or above. ${blocking.length} blocking, ${warning.length} below-gate (warn).`);
          emit("");
          emit("| Severity | Score | Advisory | Package | Version | Source |");
          emit("| -------- | ----- | -------- | ------- | ------- | ------ |");
          for (const r of rows) {
            const score = Number.isFinite(r.score) ? r.score.toFixed(1) : "—";
            const pkg = r.ecosystem ? `${r.ecosystem}:${r.name}` : r.name;
            emit(`| ${r.band} | ${score} | ${r.ids} | ${pkg} | ${r.version} | ${r.source} |`);
          }
          emit("");

          if (blocking.length > 0) {
            console.error(
              `::error::OSV advisory scan: ${blocking.length} finding(s) at or above the '${failOn}' gate. ` +
              `See the job summary for the advisory table.`,
            );
            process.exit(1);
          }
          emit(`✅ No advisory at or above the '${failOn}' gate. ${warning.length} below-gate finding(s) reported as warnings.`);
          process.exit(0);
          NODE

  # ── Aggregator: single required branch-protection context ─────────────────
  #
  # `ci-required` is the ONLY context consumers should register in their
  # branch-protection ruleset. It passes when every enabled tier succeeds (or
  # is skipped via its enable toggle). This pattern eliminates required-check-
  # name drift — the job name is defined exactly once here.
  ci-required:
    name: ci-required
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always()
    needs:
      - lint
      - typecheck
      - unit
      - contract
      - e2e
      - migration-guard
      - security
      - osv-scan
    steps:
      - name: Check all tier results
        shell: bash
        env:
          LINT_RESULT: ${{ needs.lint.result }}
          TYPECHECK_RESULT: ${{ needs.typecheck.result }}
          UNIT_RESULT: ${{ needs.unit.result }}
          CONTRACT_RESULT: ${{ needs.contract.result }}
          E2E_RESULT: ${{ needs.e2e.result }}
          MIGRATION_GUARD_RESULT: ${{ needs.migration-guard.result }}
          SECURITY_RESULT: ${{ needs.security.result }}
          OSV_SCAN_RESULT: ${{ needs.osv-scan.result }}
        run: |
          failed=()
          for tier_result in \
            "lint:${LINT_RESULT}" \
            "typecheck:${TYPECHECK_RESULT}" \
            "unit:${UNIT_RESULT}" \
            "contract:${CONTRACT_RESULT}" \
            "e2e:${E2E_RESULT}" \
            "migration-guard:${MIGRATION_GUARD_RESULT}" \
            "security:${SECURITY_RESULT}" \
            "osv-scan:${OSV_SCAN_RESULT}"; do
            tier="${tier_result%%:*}"
            result="${tier_result##*:}"
            if [ "${result}" != "success" ] && [ "${result}" != "skipped" ]; then
              failed+=("${tier}(${result})")
            fi
          done

          if [ ${#failed[@]} -gt 0 ]; then
            echo "❌ Failed tiers: ${failed[*]}" >&2
            exit 1
          fi

          echo "✅ All enabled tiers passed."
